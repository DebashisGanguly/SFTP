dr;
			inet_ntop(res->ai_family, ptr, addrstr, 100);

			break;
		}

		res = res->ai_next;
	}

	return addrstr;
}
dr;
			inet_ntop(res->ai_family, ptr, addrstr, 100);

			break;
		}

		res = res->ai_next;
	}

	return addrstr;
}
dr;
			inet_ntop(res->ai_family, ptr, addrstr, 100);

			break;
		}

		res = res->ai_next;
	}

	return addrstr;
}
dr;
			inet_ntop(res->ai_family, ptr, addrstr, 100);

			break;
		}

		res = res->ai_next;
	}

	return addrstr;
}
